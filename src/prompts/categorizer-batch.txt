You are categorizing climate-tech companies for an MBA-focused job board.

You will receive a JSON array of company entries and a climate-tech taxonomy.
Return strict JSON only.

Batch entries:
{batch_entries}

Climate-tech categories:
{categories_list}

Task:
- For each company entry, pick the single best matching category.
- If no category fits, use "None".
- Confidence must be a numeric score from 0.0 to 1.0.
- Include a short reason for each decision.
- Synthesize a 1-2 sentence "company_description" summarizing what the company does, based on its profile and keywords.

Return ONLY:
{
  "results": [
    {
      "company_id": "string",
      "category": "string",
      "confidence": 0.0,
      "reason": "string",
      "company_description": "string"
    }
  ]
}
