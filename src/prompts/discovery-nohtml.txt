Company: {company_name}
Domain: {domain}
Derived slugs already tried (all returned 404): {slugs}
Standard paths already tried (all returned 404): {tried_paths}

Based on the company name and domain, suggest a careers page URL we have NOT already tried.

Rules:
- Do NOT suggest any path listed above — they have already been confirmed as 404.
- Return a full URL with a specific path (e.g. https://example.com/work) — never return just the homepage.
- Only suggest a URL if you have reasonable confidence it exists. Otherwise return NOT_FOUND.
- Return just the URL or the string NOT_FOUND. No explanation.
