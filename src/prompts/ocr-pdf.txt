You are parsing tabular data extracted from a PitchBook company list PDF export.

The text below comes from pdftotext -layout applied to a PitchBook "Companies & Deals Screener" PDF. Multiple pages are separated by "--- PAGE BREAK ---" markers — treat them as one continuous table and return all rows in a single array. It will have irregular spacing and may contain left-navigation sidebar text (e.g. "Search Tools", "Companies", "Deals", "Investors", "Funds", "Debts & Lenders", "Alerts & Subscriptions") — ignore those words entirely; they are not column values.

Columns may appear in ANY order depending on how the user configured the export. Identify each column by its header name, not its position. Column headers may be truncated — map them as follows:
  "Financing D…" or "Last Financing D…" → Last Financing Date
  "Financing Size" or "Last Financing Si…" → Last Financing Size
  "Keyword…" or "Keyword" → Keywords
  "Companies (…)" or "Company" → Company Name

Known columns and what to extract from each (present in any order, not all may exist):
  Company Name     — required; the company's name (may include parenthetical descriptor like "(Alternative Energy Equipment)")
  Keywords         — comma-separated PitchBook keyword tags; extract exactly as shown, including any trailing ellipsis; do NOT complete or infer truncated values
  Website          — company domain (e.g. "www.example.com")
  Employees        — headcount integer
  HQ Location      — city/state or city/country
  Last Financing Date       — may be prefixed "Expected" (e.g. "Expected 01-Oct-2026" or "13-Apr-2026")
  Last Financing Deal Type  — e.g. "Seed Round", "Later Stage VC", "Merger/Acquisition"
  Last Financing Size       — most recent round size in millions (e.g. "5.00"); use null if blank
  Total Raised              — cumulative funding in millions (e.g. "1.90"); use null if blank

Rules:
- CRITICAL: Copy cell values VERBATIM from the source text. Do NOT generate, translate, paraphrase, or invent any content. If a value is not literally present in the text, use null.
- Read the actual column headers from the text to determine which column is which — do not assume a fixed order.
- One JSON object per company row. Do not merge, skip, or invent rows.
- Ignore the row number column (#).
- Use null for any cell that is empty, missing, or not applicable.
- Preserve original formatting for dates and numbers.
- If the page contains no company rows (header/filter/nav page), return [].

Return a JSON array where each element uses these exact key names:
  "Company Name", "Keywords", "Website", "Employees", "Last Financing Date", "Last Financing Deal Type",
  "Last Financing Size", "Total Raised", "HQ Location"

Return ONLY the JSON array. No markdown fences, no explanation.

--- EXTRACTED PDF TEXT ---
